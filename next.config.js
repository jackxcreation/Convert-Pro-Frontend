/** next.config.js */
const nextConfig = {
  reactStrictMode: true,
  // Remove any experimental.appDir entries
  // If you need other config, add here (but don't set experimental.appDir)
};

module.exports = nextConfig;
